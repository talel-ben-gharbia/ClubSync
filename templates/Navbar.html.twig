<nav class="navbar navbar-expand-lg navbar-custom sticky-top animate-slide-up">
    <div class="container">
        <a class="navbar-brand navbar-brand-custom" href="{{ path('app_home') }}">
            <i class="bi bi-people-fill me-2 animate-pulse"></i>ClubSync
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain" aria-controls="navbarMain" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarMain">
            <ul class="navbar-nav mx-auto">
                <li class="nav-item" data-aos="fade-down" data-aos-delay="100">
                    <a class="nav-link nav-link-custom {{ app.request.get('_route') == 'app_home' ? 'active' : '' }}" href="{{ path('app_home') }}">
                        <i class="bi bi-house-door me-1"></i> Home
                    </a>
                </li>
                <li class="nav-item" data-aos="fade-down" data-aos-delay="200">
                    <a class="nav-link nav-link-custom {{ app.request.get('_route') == 'app_club_show2' ? 'active' : '' }}" href="{{ path('app_club_show2') }}">
                        <i class="bi bi-people me-1"></i> Clubs
                    </a>
                </li>
                <li class="nav-item" data-aos="fade-down" data-aos-delay="300">
                    <a class="nav-link nav-link-custom {{ app.request.get('_route') == 'app_event_list_public' ? 'active' : '' }}" href="{{ path('app_event_list_public') }}">
                        <i class="bi bi-calendar-event me-1"></i> Events
                    </a>
                </li>
                <li class="nav-item" data-aos="fade-down" data-aos-delay="400">
                    <a class="nav-link nav-link-custom" href="#">
                        <i class="bi bi-envelope me-1"></i> Contact
                    </a>
                </li>
                {% if is_granted('ROLE_USER') %}
                    <li class="nav-item" data-aos="fade-down" data-aos-delay="500">
                        <a class="nav-link nav-link-custom {{ app.request.get('_route') starts with 'app_user_dashboard' ? 'active' : '' }}" href="{{ path('app_user_dashboard') }}">
                            <i class="bi bi-speedometer2 me-1"></i> My Dashboard
                        </a>
                    </li>
                {% endif %}
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item" data-aos="fade-down" data-aos-delay="600">
                        <a class="nav-link nav-link-custom {{ app.request.get('_route') == 'app_dashboard' ? 'active' : '' }}" href="{{ path('app_dashboard') }}">
                            <i class="bi bi-speedometer2 me-1"></i> Admin Dashboard
                        </a>
                    </li>
                {% endif %}
                {% if app.user and attribute(app.user, 'clubRole') is defined and app.user.clubRole == 'manager' %}
                    <li class="nav-item" data-aos="fade-down" data-aos-delay="700">
                        <a class="nav-link nav-link-custom {{ app.request.get('_route') == 'app_club_manage' ? 'active' : '' }}" href="{{ path('app_club_manage', {'id': app.user.club.id}) }}">
                            <i class="bi bi-gear me-1"></i> Club Manage
                        </a>
                    </li>
                {% endif %}
            </ul>
            <div class="d-flex gap-2 navbar-nav-buttons">
                {% if app.user %}
                    <div class="dropdown" data-aos="fade-left">
                        <button class="btn btn-nav btn-nav-outline dropdown-toggle shine-effect" type="button" id="userDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="bi bi-person-circle me-1"></i> {{ app.user.username }}
                        </button>
                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                            <li><a class="dropdown-item" href="{{ path('app_user_dashboard') }}"><i class="bi bi-speedometer2 me-2"></i> Dashboard</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_user_dashboard_profile') }}"><i class="bi bi-person me-2"></i> Profile</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item text-danger" href="{{ path('app_logout') }}"><i class="bi bi-box-arrow-right me-2"></i> Logout</a></li>
                        </ul>
                    </div>
                {% else %}
                    <a href="{{ path('app_register') }}" class="btn btn-nav btn-nav-outline shine-effect" data-aos="fade-left" data-aos-delay="100">Sign up</a>
                    <a href="{{ path('app_login') }}" class="btn btn-nav btn-nav-primary shine-effect" data-aos="fade-left" data-aos-delay="200">Login</a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>
